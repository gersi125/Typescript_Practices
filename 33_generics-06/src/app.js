function createCourseGoal(title, desc, date) {
    let courseGoal = {};
    courseGoal.title = title;
    courseGoal.desc = desc;
    courseGoal.completeUntil = date;
    return courseGoal;
}
const names = ['Gersi', 'Kana'];
//names.push('Ili');
